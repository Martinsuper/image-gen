{
  "name": "image-gen",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based grayscale PNG converter.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css app.js manifest.webmanifest sw.js icons dist/"
  }
}
